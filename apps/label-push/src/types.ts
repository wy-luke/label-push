export enum ConfigOptions {
  Always = "Always",
  Never = "Never",
  Suggest = "Suggest",
}

export enum DialogPick {
  Yes,
  No,
  Cancel,
}

export enum LogType {
  Info = "Info",
  Warn = "Warn",
  Error = "Error",
}
