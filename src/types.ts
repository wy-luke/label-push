export enum ConfigOptions {
  Always = 'Always',
  Never = 'Never',
  Suggest = 'Suggest',
}

export enum DialogPick {
  Yes,
  No,
  Cancle,
}

export enum LogType {
  Info = 'Info',
  Warn = 'Warn',
  Error = 'Error',
}
